default_install_hook_types: [pre-commit]

repos:
  - repo: local
    hooks:
      - id: validate
        name: Full validation (ruff, mypy, pytest)
        entry: python scripts/validate.py
        language: system
        pass_filenames: false
        always_run: true
