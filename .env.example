# Optional overrides — copy to .env only when you need to change defaults.
# All other settings use values from app/config.py (Settings).
#
# LLM_CONTEXT_SIZE=32768
# LLM_CODE_CONTEXT_SIZE=32768
# LLM_CODE_MODEL_PATH=models/qwen2.5-coder-1.5b-instruct-q5_k_m.gguf
# SELF_IMPROVE_PLAN_MAX_TOKENS=8192
# SELF_IMPROVE_APPLY_MAX_ATTEMPTS=4

GITHUB_PR_VERIFY_COMMAND=python -m pytest -q -p no:cacheprovider --basetemp=.pytest-tmp
